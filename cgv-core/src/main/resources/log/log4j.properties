# In SMCertLogger class, need to load this files with code. by default the log level is info, but user can specify the log level in command line.
# it assumed that this file is only is used when the tool is integrate in SM
# because if it is an independent tool, only java logging can be used.